<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style BasedOn="{StaticResource {x:Type Button}}"
           TargetType="Button"
           x:Key="PurpleButton">
        <Style.Setters>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                        Name="brd"
                        CornerRadius="15"
                            BorderThickness="2"
                            BorderBrush="Black">
                            <Border.Background>
                                <LinearGradientBrush>
                                    <GradientStop 
                                        Color="#ac61bb" Offset="0.0"
                                        />
                                    <GradientStop 
                                        Color="#ed6372" Offset="1"
                                        />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Grid VerticalAlignment="Stretch"
                                  HorizontalAlignment="Stretch">
                                <TextBlock
                                    Name="txt"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    FontSize="20"
                                    Foreground="#FFFFFF"
                                    Text="{TemplateBinding Content}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="brd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush>
                                            <GradientStop 
                                        Color="#ed6372" Offset="0.0"
                                        />
                                            <GradientStop 
                                        Color= "#b28154" Offset="0.5"
                                        />
                                            <GradientStop 
                                        Color="#ac61bb" Offset="1"
                                        />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="brd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush>
                                            <GradientStop 
                                        Color="#bf9fa2" Offset="0.0"
                                        />
                                            <GradientStop 
                                        Color= "#a8998a" Offset="0.5"
                                        />
                                            <GradientStop 
                                        Color="#9c84a1" Offset="1"
                                        />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="txt" Property="Foreground" Value="#b3adb3">
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style BasedOn="{StaticResource {x:Type Button}}"
           TargetType="Button"
           x:Key="DefoultButton">
        <Style.Setters>
            <Setter Property="Background" Value="#252930"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Padding="5" x:Name="brd" Background="{TemplateBinding Background}">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="{TemplateBinding FontSize}"
                                       Text="{TemplateBinding Content}"
                                       />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="brd" Property="Background" Value="#3c4452"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
</ResourceDictionary>